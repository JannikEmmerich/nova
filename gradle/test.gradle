dependencies {
  testImplementation "org.springframework.boot:spring-boot-starter-test:${springBootVersion}"
  testImplementation 'io.projectreactor:reactor-test'
}

testing {
  suites {
    test {
      useJUnitJupiter() // or useJUnit(), useTestNG(), etc.
    }
  }
}
